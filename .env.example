# Vistructum Beispiel-Konfiguration (nach .env kopieren, .env bleibt lokal)
SIDECAR_HOST=127.0.0.1
SIDECAR_PORT=8000
MODEL_DIR=/models
ORT_THREADS=1
OUTBOX_PATH=plugins/vistructum/outbox.db
POLL_INTERVAL_SECONDS=2
