name: vistructum
version: '${project.version}'
main: de.kylekreuter.vistructum.core.VistructumCore
api-version: '1.21'
author: Kyle Kreuter
description: Detects swastikas built from blocks and publishes findings through the Vistructum API.
libraries:
  - org.xerial:sqlite-jdbc:${sqlite-jdbc.version}
  - com.microsoft.onnxruntime:onnxruntime:${onnxruntime.version}
